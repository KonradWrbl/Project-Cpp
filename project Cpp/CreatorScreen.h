#pragma once
#include "Screen.h"
#include "SFML/Graphics.hpp"
#include <vector>

class CreatorScreen :public Screen
{
public:
	CreatorScreen();

	////////////////////////////////////////////////////////////
	//	Method draws the main screen on the window
	////////////////////////////////////////////////////////////
	virtual void draw(sf::RenderWindow*) override;

	////////////////////////////////////////////////////////////
	//	Method runs on event
	////////////////////////////////////////////////////////////
	virtual void onEvent(sf::Event event) override;
	virtual int getIndex() override;

	////////////////////////////////////////////////////////////
	//	Method saves generated maze to file
	////////////////////////////////////////////////////////////
	void saveToFile();

	////////////////////////////////////////////////////////////
	//	Method set the random size of new maze
	////////////////////////////////////////////////////////////
	void setRandomSize();

	////////////////////////////////////////////////////////////
	//	Method generates the new map
	////////////////////////////////////////////////////////////
	void setMazeTab();

	////////////////////////////////////////////////////////////
	//	Getters
	////////////////////////////////////////////////////////////
	int getMapWidth();
	int getMapHeight();
	bool getMazeTab(int x, int y);

private:
	std::vector<std::vector<int>> mazeMap;
	int width;
	int height;
};

