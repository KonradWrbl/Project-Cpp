#pragma once
#include <vector>
#include "Screen.h"
#include "Map.h"
class Game
{
public:

	////////////////////////////////////////////////////////////
	//	Method makes game included screens
	////////////////////////////////////////////////////////////
	Game(std::vector<Screen*> screen);

	////////////////////////////////////////////////////////////
	//	Method is wathing for an events on the app window
	////////////////////////////////////////////////////////////
	void start();
private:
	std::vector<Screen*> screen;
	Map map;
};

